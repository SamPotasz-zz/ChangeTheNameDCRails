<%= form_for(@user) do |f| %>


  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  	<%= f.label :full_name, "Full Name:" %> <%= f.text_field :full_name %> <br/>	
	<%= f.label :email, "E-Mail Address:" %> <%= f.text_field :email %><br/>
	<%= f.label :affilation, "How are you associated with the Redskins? A lifelong fan? A casual fan? A 3-time Super Bowl winning coach?" %> <br/>
	<%= f.text_area :affiliation %><br/>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
